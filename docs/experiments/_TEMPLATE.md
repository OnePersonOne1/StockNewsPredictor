# EXP-?: <제목>

| 항목 | 내용 |
|---|---|
| 상태 | 계획 / 진행 / 완료 / 폐기 |
| 날짜 | YYYY-MM-DD |
| 커밋 | `<해시>` |
| 데이터 | <프로필/필터/연도> |
| 산출물 | `<경로>` |

## 1. 동기 / 가설
무엇을 왜 시험하는가. 직전 실험의 어떤 한계를 겨냥하는가.

## 2. 설정
모델·하이퍼파라미터·split·전처리. **고정 설계**(encoder=klue/roberta-base, ε=0.3σ,
σ-train-only, temporal split)에서 무엇만 바꿨는지 명시.

## 3. 결과
표(test macro-F1 등) + 필요한 confusion / 지표. 산출 아티팩트 경로.

## 4. 분석
왜 그런 결과가 났는가. 근거(혼동행렬·분포·통계)와 함께. **실패도 원인까지.**

## 5. 한계
표본·기간·전처리·측정의 약점. 결과 해석에 붙일 caveat.

## 6. 결론 / 다음
보고서에 넣을 메시지 한두 줄 + 이 결과가 시사하는 다음 실험.

## 7. 재현
```
<명령>
```
